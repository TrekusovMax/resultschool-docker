services:
  date:
    image: trekusovmax/result-date
    build:
      dockerfile: date.dockerfile
  result:
    image: trekusovmax/result
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    depends_on:
      - date
